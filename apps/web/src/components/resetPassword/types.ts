export interface FormValues {
  email: string;
  password: string;
  password1: string;
}

export interface FormProps {
  initialEmail?: string;
  initialPassword?: string;
  initialPassword1?: string;
}
